% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_pairs.R
\name{explore_pairs}
\alias{explore_pairs}
\title{Scatter-plot matrix}
\usage{
explore_pairs(
  data,
  cols = NULL,
  group = NULL,
  max_cols = 8,
  point_alpha = 0.5,
  method = c("pearson", "spearman", "kendall"),
  palette = NULL,
  title = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Numeric columns to include. If \code{NULL}, every numeric column is
used, so a wide frame needs \code{cols} to keep within \code{max_cols}.}

\item{group}{Optional grouping variable mapped to colour.}

\item{max_cols}{Largest number of variables accepted, since a k-by-k matrix
grows quickly. More than this is an error, so the matrix never quietly
drops a variable.}

\item{point_alpha}{Point transparency in the scatter panels.}

\item{method}{Correlation method for the upper-triangle coefficients, passed
to \code{\link[stats:cor]{stats::cor()}}: \code{"pearson"}, \code{"spearman"} or \code{"kendall"}. A pair whose
correlation is undefined (a constant column, or too few complete cases) is
labelled \code{n/a}, as in \code{\link[=correlation_heatmap]{correlation_heatmap()}}.}

\item{palette}{Colours for the groups; defaults to \code{\link[=depictr_palette]{depictr_palette()}}.}

\item{title}{Overall title for the matrix.}
}
\value{
A 'patchwork' object (printable like a \link[ggplot2:ggplot]{ggplot2::ggplot}).
}
\description{
A matrix of pairwise plots for a set of numeric variables: scatter plots
below the diagonal, densities on the diagonal and correlation coefficients
above the diagonal. Optionally colour everything by a grouping variable.
Built with 'patchwork', so it shares the package theme and palette.
}
\examples{
explore_pairs(crop_yield, cols = c("rainfall", "fertiliser", "yield"))
\donttest{
explore_pairs(crop_yield, cols = c("rainfall", "fertiliser", "yield"),
              group = treatment, method = "spearman")
}
}
