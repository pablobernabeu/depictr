% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_pairs.R
\name{explore_pairs}
\alias{explore_pairs}
\title{Scatter-plot matrix}
\usage{
explore_pairs(
  data,
  cols = NULL,
  group = NULL,
  max_cols = 8,
  point_alpha = 0.5,
  palette = NULL,
  title = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Numeric columns to include. If \code{NULL}, all numeric columns are
used (up to \code{max_cols}).}

\item{group}{Optional grouping variable mapped to colour.}

\item{max_cols}{Safety cap on the number of variables (a k-by-k matrix grows
quickly).}

\item{point_alpha}{Point transparency in the scatter panels.}

\item{palette}{Colours for the groups; defaults to \code{\link[=depictr_palette]{depictr_palette()}}.}

\item{title}{Overall title for the matrix.}
}
\value{
A 'patchwork' object (printable like a \link[ggplot2:ggplot]{ggplot2::ggplot}).
}
\description{
A matrix of pairwise plots for a set of numeric variables: scatter plots
below the diagonal, densities on the diagonal and correlation coefficients
above the diagonal. Optionally colour everything by a grouping variable.
Built with 'patchwork', so it shares the package theme and palette.
}
\examples{
explore_pairs(crop_yield, cols = c("rainfall", "fertilizer", "yield"))
\donttest{
explore_pairs(crop_yield, cols = c("rainfall", "fertilizer", "yield"),
              group = treatment)
}
}
