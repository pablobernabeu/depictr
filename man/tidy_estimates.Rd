% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_estimates.R
\name{tidy_estimates}
\alias{tidy_estimates}
\title{Extract a tidy table of estimates}
\usage{
tidy_estimates(x, conf_level = 0.95, ...)
}
\arguments{
\item{x}{A fitted model or a data frame of results.}

\item{conf_level}{Confidence (or credible) level for the interval.}

\item{...}{Passed to methods (and to \code{\link[broom:reexports]{broom::tidy()}} in the fallback). The
\code{merMod} method additionally accepts \code{effects}, which currently only
supports \code{"fixed"}.}
}
\value{
A data frame with one row per term and the columns \code{term},
\code{estimate}, \code{std.error}, \code{conf.low} and \code{conf.high}.
}
\description{
\code{tidy_estimates()} turns the output of a model (or an existing data frame of
results) into a single standardised table with the columns \code{term},
\code{estimate}, \code{std.error}, \code{conf.low} and \code{conf.high}. It is the common
currency used by \code{\link[=coefficient_plot]{coefficient_plot()}} and \code{\link[=compare_models]{compare_models()}}, but is useful
on its own.
}
\details{
Methods are provided for \code{lm}, \code{glm} and \code{merMod} (mixed models fitted with
'lme4') objects, and for data frames. For any other model class the function
falls back to \code{\link[broom:reexports]{broom::tidy()}} when the 'broom' package is installed.

Confidence intervals are computed with the normal approximation
(estimate +/- z * standard error) for \code{glm} and \code{merMod} objects, which is
fast and dependency-free; \code{lm} objects use the exact t-based interval. Supply
a data frame with your own intervals (for example profiled or posterior
intervals) to override this.
}
\examples{
fit <- lm(yield ~ rainfall + fertilizer + treatment, data = crop_yield)
tidy_estimates(fit)

# A data frame of pre-computed estimates is standardised, not re-fitted:
df <- data.frame(
  parameter = c("a", "b"),
  Estimate = c(0.2, -0.4),
  "2.5 \%" = c(0.1, -0.6),
  "97.5 \%" = c(0.3, -0.2),
  check.names = FALSE
)
tidy_estimates(df)
}
