% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dumbbell_plot.R
\name{dumbbell_plot}
\alias{dumbbell_plot}
\title{Dumbbell plot}
\usage{
dumbbell_plot(
  data,
  category,
  value,
  group,
  sort = c("gap", "value", "none"),
  point_size = 3,
  palette = NULL,
  legend_inside = FALSE,
  title = NULL,
  x_lab = NULL,
  y_lab = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{category}{The categorical axis (string or unquoted name); one row of
the plot per level.}

\item{value}{The numeric value to compare (string or unquoted name).}

\item{group}{The two-level grouping variable whose levels are the two ends of
each dumbbell (string or unquoted name).}

\item{sort}{How to order the categories up the axis: \code{"gap"} (by the signed
difference between the two groups, the default), \code{"value"} (by the second
group's value), or \code{"none"} (the data's own order).}

\item{point_size}{Size of the end points.}

\item{palette}{Length-2 colours for the two groups; defaults to
\code{\link[=depictr_palette]{depictr_palette()}}.}

\item{legend_inside}{When \code{TRUE}, draw the two-group legend inside the panel
(in the top-right corner, which the default gap sort with the shortest
dumbbell on top usually leaves clear) over a translucent background,
instead of in a right-hand margin. Defaults to \code{FALSE}.}

\item{title, x_lab, y_lab}{Plot title and axis labels.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Compares a numeric value between exactly two groups across a set of
categories: for each category the two group values are drawn as points joined
by a connecting segment, so the size and direction of the gap is read at a
glance. It is a clearer alternative to paired or grouped bars for
before/after, two-condition or two-period comparisons.
}
\details{
When a category has several rows per group the values are averaged. Rows with
a missing category, value or group are dropped.
}
\examples{
wb <- wellbeing_survey
wb$age_group <- ifelse(wb$age < median(wb$age), "younger", "older")
dumbbell_plot(wb, region, life_satisfaction, age_group)
}
