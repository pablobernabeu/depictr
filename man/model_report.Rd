% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_report.R
\name{model_report}
\alias{model_report}
\title{A one-figure model report}
\usage{
model_report(model, predictor = NULL, title = NULL, subtitle = NULL)
}
\arguments{
\item{model}{A fitted \code{lm} or \code{glm} model.}

\item{predictor}{Focal predictor for the effect panel. If \code{NULL}, the first
numeric predictor (or, failing that, the first predictor) is used.}

\item{title}{Overall title.}

\item{subtitle}{Overall subtitle. If \code{NULL}, a line of fit statistics
(number of observations, R-squared and AIC) is used.}
}
\value{
A 'patchwork' object (printable like a \link[ggplot2:ggplot]{ggplot2::ggplot}).
}
\description{
Assembles a compact, consistent overview of a fitted model in a single
figure: the coefficient estimates, the predicted effect of a focal predictor,
a residuals-vs-fitted plot and a normal Q-Q plot, with a subtitle of key
fit statistics. It is a convenience wrapper that composes several statviz
plots with \code{\link[=arrange_plots]{arrange_plots()}}, and is handy for a quick model review or a
report appendix.
}
\examples{
fit <- lm(yield ~ rainfall + fertilizer + soil_ph + treatment,
          data = crop_yield)
model_report(fit, title = "Crop-yield model")

gfit <- glm(accuracy ~ word_frequency + RT + condition,
            data = lexical_decision, family = binomial)
model_report(gfit)
}
