% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecdf_plot.R
\name{ecdf_plot}
\alias{ecdf_plot}
\title{Empirical cumulative distribution function (ECDF) plot}
\usage{
ecdf_plot(
  data,
  x,
  group = NULL,
  reference_quantiles = NULL,
  palette = NULL,
  title = NULL,
  x_lab = NULL,
  y_lab = "Cumulative proportion"
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{The numeric variable (string or unquoted name).}

\item{group}{Optional grouping variable (string or unquoted name) mapped to
colour, giving one ECDF per group.}

\item{reference_quantiles}{Optional numeric vector of probabilities in
[0, 1] to mark with light horizontal guides (e.g. \code{c(0.25, 0.5, 0.75)});
\code{NULL} (the default) draws none.}

\item{palette}{Colours for the groups; defaults to \code{\link[=depictr_palette]{depictr_palette()}}.}

\item{title, x_lab, y_lab}{Plot title and axis labels.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws the empirical CDF of a numeric variable -- the proportion of
observations at or below each value -- optionally split by a grouping
variable. Unlike a histogram it needs no bin-width choice and makes
quantiles, medians and group shifts easy to read directly off the curve.
}
\examples{
ecdf_plot(lexical_decision, RT)
ecdf_plot(lexical_decision, RT, group = condition,
          reference_quantiles = c(0.25, 0.5, 0.75))
}
