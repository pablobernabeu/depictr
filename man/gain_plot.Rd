% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gain_lift.R
\name{gain_plot}
\alias{gain_plot}
\title{Cumulative gains chart}
\usage{
gain_plot(x, score = NULL, colour = "#005b96", title = NULL)
}
\arguments{
\item{x}{A binomial \code{glm}, or the vector of observed outcomes (0/1, logical
or a two-level factor with the positive class second).}

\item{score}{When \code{x} is an outcome vector, the matching scores or predicted
probabilities.}

\item{colour}{Curve colour.}

\item{title}{Plot title.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Shows how many of the positive cases are captured as more of the
population is targeted in order of predicted score -- the standard chart for
judging a classifier's value for ranking and targeting (marketing, triage,
fraud). The diagonal is the no-model baseline; the upper envelope is a
perfect model.
}
\examples{
gfit <- glm(accuracy ~ word_frequency + RT + condition,
            data = lexical_decision, family = binomial)
gain_plot(gfit)
}
