% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_fit_table.R
\name{model_fit_table}
\alias{model_fit_table}
\title{Goodness-of-fit statistics across models}
\usage{
model_fit_table(..., digits = 3)
}
\arguments{
\item{...}{Two or more fitted models. Name the arguments to label the rows.}

\item{digits}{Number of decimal places to round to.}
}
\value{
A data frame with one row per model and columns \code{model}, \code{n}, \code{df},
\code{AIC}, \code{BIC}, \code{logLik}, \code{R2} and \code{RMSE}.
}
\description{
Collects the usual model-comparison statistics into one tidy data frame, one
row per model. These comprise the number of observations, the degrees of
freedom, AIC, BIC, the log-likelihood, an R-squared (ordinary for \code{lm} and
McFadden's pseudo-R-squared for \code{glm}) and the root-mean-square error.
}
\examples{
m1 <- lm(yield ~ rainfall, data = crop_yield)
m2 <- lm(yield ~ rainfall + fertiliser, data = crop_yield)
m3 <- lm(yield ~ rainfall + fertiliser + soil_ph + treatment,
         data = crop_yield)
model_fit_table(simple = m1, medium = m2, full = m3)
}
\references{
\insertRef{mcfadden1974}{depictr}
}
