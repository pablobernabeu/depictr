% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeseries.R
\name{timeseries_plot}
\alias{timeseries_plot}
\title{Time-series plot}
\usage{
timeseries_plot(
  x,
  time = NULL,
  value = NULL,
  group = NULL,
  rolling = NULL,
  forecast = NULL,
  frequency = NULL,
  level = 0.95,
  palette = NULL,
  point = FALSE,
  title = NULL,
  x_lab = NULL,
  y_lab = NULL
)
}
\arguments{
\item{x}{A \code{ts} object, a numeric vector, or a data frame.}

\item{time}{When \code{x} is a data frame, the time column (string or unquoted
name); when \code{x} is a numeric vector, an optional vector of times.}

\item{value}{When \code{x} is a data frame, the value column.}

\item{group}{When \code{x} is a data frame, an optional grouping column mapped to
colour.}

\item{rolling}{Optional integer window for a centred moving-average overlay.
The data are ordered by time (within each group) before the moving average
is computed, so unsorted input is handled correctly.}

\item{forecast}{Optional forecast overlay for a single (non-grouped) series.
Either an integer horizon (number of future steps) to forecast with the
built-in STL + seasonal-naive-with-drift method (see \code{\link[=ts_forecast]{ts_forecast()}}), or a
pre-computed forecast supplied as a data frame with columns \code{time}, \code{fit}
and (optionally) \code{lwr}/\code{upr}, for instance from a fitted
\code{forecast::forecast()} object. The point forecast continues the line and a
shaded prediction-interval ribbon, which widens with the horizon, is drawn
behind it.}

\item{frequency}{Number of observations per period, used only to coerce a
numeric \code{x} to a \code{ts} when an integer \code{forecast} horizon is requested.}

\item{level}{Prediction-interval coverage for the built-in forecast (a single
number strictly between 0 and 1, e.g. \code{0.95}).}

\item{palette}{Colours for the groups; defaults to \code{\link[=depictr_palette]{depictr_palette()}}.}

\item{point}{Whether to add points as well as the line.}

\item{title, x_lab, y_lab}{Title and axis labels.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Plots one or more series over time, with an optional moving-average overlay.
The input can be a \code{ts} object, a numeric vector, or a data frame with a time
column, a value column and an optional grouping column.
}
\examples{
timeseries_plot(AirPassengers, rolling = 12,
                title = "Air passengers", y_lab = "Passengers")
# 24-month forecast with a 90\% prediction interval
timeseries_plot(AirPassengers, forecast = 24, level = 0.9)
}
\seealso{
\code{\link[=ts_forecast]{ts_forecast()}}, \code{\link[=decompose_plot]{decompose_plot()}}, \code{\link[=seasonal_plot]{seasonal_plot()}}
}
