% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{lexical_decision}
\alias{lexical_decision}
\title{Simulated lexical-decision experiment}
\format{
A data frame with 960 rows and 7 variables:
\describe{
\item{participant}{Participant identifier (factor, 24 levels).}
\item{item}{Item identifier (factor, 40 levels).}
\item{condition}{Priming condition (factor): \code{related} or \code{unrelated}.}
\item{modality}{Presentation modality (factor): \code{visual} or \code{auditory}.}
\item{word_frequency}{Item-level word frequency on the Zipf scale.}
\item{RT}{Reaction time, in milliseconds.}
\item{accuracy}{Response accuracy: 1 correct, 0 incorrect.}
}
}
\source{
Simulated; see \code{data-raw/generate_datasets.R}.
}
\usage{
lexical_decision
}
\description{
A reproducibly simulated psycholinguistic lexical-decision experiment, in
which participants judge whether letter strings are real words. The design
is fully crossed and counterbalanced: 24 participants each respond to all 40
items once (960 trials). \code{condition} (related/unrelated priming) and
\code{modality} (visual/auditory) are both within-participant and within-item,
assigned by a Latin-square list rotation so that, across participants, every
item appears equally often in each condition and in each modality. Neither
factor is therefore collinear with the item random intercept, which makes
this a clean fit for crossed-random-effects models such as
\code{lmer(RT ~ condition + modality + word_frequency + (1 | participant) + (1 | item))}, and hence for \code{\link[=optimizer_fixef_plot]{optimizer_fixef_plot()}}, \code{\link[=coefficient_plot]{coefficient_plot()}}
and the distribution plots.
}
\details{
\code{RT} is generated log-normally from participant and item random intercepts
plus the fixed effects (unrelated priming about 35 ms slower; auditory a
touch slower; more frequent words faster), with a 250 ms floor; RT analyses
should use correct trials (\code{accuracy == 1}). \code{accuracy} is generated from
the exogenous predictors (\code{word_frequency}, \code{condition}, \code{modality}) and
random effects only - it is not derived from \code{RT} - so classification demos
built on it are not circular.

The data are synthetic, generated by \code{data-raw/generate_datasets.R} with a
fixed seed; they do not describe any real individuals.
}
\keyword{datasets}
