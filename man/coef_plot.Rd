% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coef_plot.R
\name{coef_plot}
\alias{coef_plot}
\title{Forest (coefficient) plot}
\usage{
coef_plot(
  x,
  conf_level = 0.95,
  intercept = FALSE,
  order = c("none", "ascending", "descending"),
  labels = NULL,
  interaction = c("times", "asterisk", "colon", "space"),
  point_colour = "#005b96",
  reference_colour = "grey60",
  reference_line = 0,
  point_size = 2.2,
  line_size = 0.7,
  title = NULL,
  subtitle = NULL,
  x_lab = "Estimate"
)
}
\arguments{
\item{x}{A fitted model or a tidy data frame of estimates.}

\item{conf_level}{Confidence/credible level, passed to \code{\link[=tidy_estimates]{tidy_estimates()}}
when \code{x} is a model.}

\item{intercept}{Keep the intercept term? Defaults to \code{FALSE}, as the
intercept is rarely of interest on a forest plot and its scale often
dwarfs the other terms.}

\item{order}{Order the terms by estimate: \code{"none"} (keep input order),
\code{"ascending"} or \code{"descending"}.}

\item{labels}{Optional display labels for the terms. Either a character
vector the same length as the number of terms (in plotting order) or a
named vector mapping raw term names to labels. If \code{NULL}, names are tidied
with \code{\link[=format_terms]{format_terms()}}.}

\item{interaction}{Passed to \code{\link[=format_terms]{format_terms()}} to control how interaction
terms are rendered (ignored when \code{labels} is supplied).}

\item{point_colour, reference_colour}{Colours for the estimates and the
reference line.}

\item{reference_line}{Position of a vertical reference line (e.g. \code{0} for
differences, \code{1} for odds/risk ratios). Use \code{NA} to omit it.}

\item{point_size, line_size}{Size of the points and interval lines.}

\item{title, subtitle, x_lab}{Plot title, subtitle and x-axis label.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws a horizontal point-and-interval ("forest") plot of model estimates.
The input can be a fitted model (anything \code{\link[=tidy_estimates]{tidy_estimates()}} understands) or
a data frame of pre-computed estimates.
}
\examples{
fit <- lm(yield ~ rainfall + fertilizer + soil_ph + treatment,
          data = crop_yield)
coef_plot(fit)

# Order terms and add a title
coef_plot(fit, order = "descending", title = "Drivers of crop yield")
}
