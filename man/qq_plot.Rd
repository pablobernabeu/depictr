% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics_extra.R
\name{qq_plot}
\alias{qq_plot}
\title{Normal quantile-quantile plot}
\usage{
qq_plot(
  x,
  colour = depictr_brand(),
  title = NULL,
  x_lab = "Theoretical quantiles",
  y_lab = NULL,
  band = TRUE,
  band_type = c("pointwise", "simulate"),
  level = 0.95,
  n_sim = 1000,
  band_fill = depictr_reference(),
  seed = NULL
)
}
\arguments{
\item{x}{A numeric vector, or a fitted \code{lm}/\code{glm} model (its standardised
residuals are used).}

\item{colour}{Point colour. Defaults to the depictr brand blue.}

\item{title, x_lab, y_lab}{Title and axis labels.}

\item{band}{Whether to draw a confidence band/envelope.}

\item{band_type}{Band construction: \code{"pointwise"} (analytic order-statistic
standard errors, the default) or \code{"simulate"} (a Monte-Carlo envelope).}

\item{level}{Confidence level for the band.}

\item{n_sim}{Number of simulations for \code{band_type = "simulate"}.}

\item{band_fill}{Fill colour of the band.}

\item{seed}{Optional integer seed for the simulated envelope, for
reproducibility.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
A normal Q-Q plot for a numeric vector or for the standardised residuals of a
fitted model, with a reference line and an optional confidence band that makes
departures from normality easier to judge. Points that stray outside the band
are unusual under normality; under a normal sample roughly \code{level} of the
points fall inside a pointwise band.
}
\details{
Two band constructions are offered. \code{"pointwise"} is analytic: it uses the
large-sample standard error of the \eqn{i}-th order statistic,
\eqn{\mathrm{se} = \frac{\hat\sigma}{\phi(z_i)}\sqrt{p_i(1-p_i)/n}}, around
the fitted reference line. \code{"simulate"} builds a Monte-Carlo envelope by
repeatedly drawing normal samples of the same size and taking the empirical
quantiles of the simulated order statistics, which needs no large-sample
approximation.
}
\examples{
qq_plot(rnorm(100))
qq_plot(rt(100, df = 3), band_type = "simulate")
fit <- lm(yield ~ rainfall + fertilizer, data = crop_yield)
qq_plot(fit)
}
