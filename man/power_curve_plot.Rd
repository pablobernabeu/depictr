% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_curve_plot.R
\name{power_curve_plot}
\alias{power_curve_plot}
\title{Plot a power analysis curve}
\usage{
power_curve_plot(
  x,
  target = 0.8,
  x_lab = "Sample size",
  x_breaks = NULL,
  x_expand = NULL,
  ribbon = TRUE,
  title = NULL,
  interaction = c("times", "asterisk", "colon", "space")
)
}
\arguments{
\item{x}{A \code{powerCurve} object from 'simr', or a data frame with a sample
size column (\code{nlevels}, \code{n} or \code{x}), a power column (\code{mean} or \code{power}),
and optional \code{lower}/\code{upper} confidence limits.}

\item{target}{Target power, drawn as a horizontal reference line. Use \code{NA}
to omit it.}

\item{x_lab}{X-axis label.}

\item{x_breaks}{Approximate number of x-axis breaks.}

\item{x_expand}{Optional value(s) to extend the x-axis to.}

\item{ribbon}{Draw the confidence band as a shaded ribbon (\code{TRUE}) or as
error bars (\code{FALSE})?}

\item{title}{Plot title. If \code{NULL} and \code{x} is a 'simr' power curve, the
predictor name stored in the object is used.}

\item{interaction}{Passed to \code{\link[=format_terms]{format_terms()}} when deriving the title from a
'simr' object.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws statistical power against sample size, with a dashed line at a target
power (80\% by default). The input is usually a power curve produced by
\code{simr::powerCurve()}, but a tidy data frame works too, so the plot can be
redrawn without re-running a (often slow) power simulation.
}
\details{
This refactors the original \code{powercurvePlot()} gist.
}
\examples{
pc <- data.frame(
  nlevels = c(10, 20, 30, 40, 50, 60),
  mean = c(0.18, 0.34, 0.52, 0.66, 0.79, 0.88),
  lower = c(0.10, 0.25, 0.42, 0.56, 0.70, 0.81),
  upper = c(0.28, 0.44, 0.62, 0.75, 0.86, 0.93)
)
power_curve_plot(pc, title = "Power for the condition effect")
}
