% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification.R
\name{roc_curve_plot}
\alias{roc_curve_plot}
\title{ROC curve}
\usage{
roc_curve_plot(x, score = NULL, colour = depictr_brand(), title = NULL)
}
\arguments{
\item{x}{A binomial \code{glm}, or the vector of observed outcomes (0/1, logical
or a two-level factor with the positive class second).}

\item{score}{When \code{x} is an outcome vector, the matching vector of scores or
predicted probabilities.}

\item{colour}{Curve colour. Defaults to the depictr brand blue.}

\item{title}{Plot title.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object. The AUC is also stored in
\code{attr(plot, "auc")}.
}
\description{
Plots the receiver operating characteristic (ROC) curve for a binary
classifier and reports the area under the curve (AUC). The input can be a
fitted binomial \code{glm}, or a pair of vectors: the observed binary outcome and
a continuous score (e.g. predicted probabilities).
}
\examples{
gfit <- glm(accuracy ~ word_frequency + condition + RT,
            data = lexical_decision, family = binomial)
roc_curve_plot(gfit)
}
