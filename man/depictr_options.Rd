% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{depictr_options}
\alias{depictr_options}
\title{Get or set the depictr look-and-feel options}
\usage{
depictr_options(
  base_size,
  base_family,
  brand,
  accent,
  reference,
  palette,
  na_value
)
}
\arguments{
\item{base_size}{Base font size in points for \code{\link[=theme_depictr]{theme_depictr()}}.}

\item{base_family}{Base font family for \code{\link[=theme_depictr]{theme_depictr()}}.}

\item{brand}{The depictr brand colour (used for plot titles and as the lead
qualitative colour). Returned by \code{depictr_brand()}.}

\item{accent}{A secondary highlight colour. Returned by \code{depictr_accent()}.}

\item{reference}{The colour used for reference / annotation lines. Returned
by \code{depictr_reference()}.}

\item{palette}{An optional custom qualitative palette: a character vector of
hex colours used by \code{\link[=depictr_palette]{depictr_palette()}} (type \code{"qualitative"}) and the
discrete scales in place of the built-in Okabe-Ito set. \code{NULL} restores the
built-in palette.}

\item{na_value}{Colour used for \code{NA} levels by \code{\link[=scale_colour_depictr]{scale_colour_depictr()}} and
\code{\link[=scale_fill_depictr]{scale_fill_depictr()}}.}
}
\value{
A named list of the resolved option values. When setting, the
\emph{previous} values are returned invisibly.
}
\description{
depictr reads a small set of global options so that you can configure the
shared look of every plot once, at the top of a script or in your
\code{.Rprofile}, instead of passing the same arguments to each function. The
options are honoured by \code{\link[=theme_depictr]{theme_depictr()}} (base size, base family and the
accent used for titles), by \code{\link[=depictr_palette]{depictr_palette()}} and the
\code{\link[=scale_colour_depictr]{scale_colour_depictr()}} family (an optional custom qualitative palette), and
by the colour accessors \code{depictr_brand()}, \code{depictr_accent()} and
\code{depictr_reference()}.
}
\details{
Called with no arguments, \code{depictr_options()} returns the currently resolved
values (option if set, otherwise package default). Called with named
arguments it sets the matching \verb{options(depictr.<name> = )} entries and
returns the \emph{previous} resolved values invisibly, so the pattern
\verb{old <- depictr_options(...); on.exit(do.call(depictr_options, old))} restores
them. Pass \code{NULL} for an argument to clear that option and fall back to the
package default.
}
\examples{
# Inspect the current settings
depictr_options()

# Set a larger base size and a different accent, then restore
old <- depictr_options(base_size = 14, accent = "#e69f00")
theme_depictr()              # now uses base_size 14
do.call(depictr_options, old)

# Use a custom qualitative palette everywhere
old <- depictr_options(palette = c("#1b9e77", "#d95f02", "#7570b3"))
depictr_palette(3)
do.call(depictr_options, old)
}
