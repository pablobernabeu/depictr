% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colours.R
\name{simulate_cvd}
\alias{simulate_cvd}
\title{Simulate how colours appear under a colour-vision deficiency}
\usage{
simulate_cvd(colours, deficiency, severity = 1)
}
\arguments{
\item{colours}{Character vector of colours, in any form
\code{\link[grDevices:col2rgb]{grDevices::col2rgb()}} understands.}

\item{deficiency}{The deficiency to simulate: \code{"protan"} or \code{"deutan"}
(the two red-green forms) or \code{"tritan"} (blue-yellow).}

\item{severity}{Severity in \verb{[0, 1]}. Zero leaves the colours unchanged and
one is the full deficiency. Intermediate values interpolate the transform
towards the identity, an approximation to Machado et al.'s per-severity
matrices.}
}
\value{
A character vector of lower-case hex colours, the same length as
\code{colours}.
}
\description{
Maps each colour to the colour a reader with a given form and severity of
colour-vision deficiency would perceive, using the physiologically-based
model of Machado, Oliveira and Fernandes (2009). The transformation is
defined on linear-light RGB, so a colour is decoded from sRGB to linear RGB
(the IEC 61966-2-1 transfer functions), transformed, then re-encoded.
}
\details{
This is the simulator behind \code{\link[=palette_preview]{palette_preview()}}'s \code{cvd} argument, behind
\code{\link[=palette_safety]{palette_safety()}}, and behind the colour-separability rows of
\code{\link[=check_figure]{check_figure()}}.
}
\examples{
simulate_cvd(c("#005b96", "#e69f00"), "deutan")

# Half severity moves the colours only part of the way.
simulate_cvd(c("#005b96", "#e69f00"), "deutan", severity = 0.5)
}
\references{
\insertRef{machado2009}{depictr}
}
