% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coefficient_plot.R
\name{coefficient_plot}
\alias{coefficient_plot}
\title{Forest (coefficient) plot}
\usage{
coefficient_plot(
  x,
  conf_level = 0.95,
  intercept = FALSE,
  order = c("none", "ascending", "descending"),
  labels = NULL,
  interaction = c("times", "asterisk", "colon", "space"),
  point_colour = depictr_brand(),
  reference_colour = depictr_reference(),
  reference_line = 0,
  point_size = 2.2,
  line_size = 0.7,
  facet = FALSE,
  scales = c("fixed", "free"),
  standardize = FALSE,
  title = NULL,
  subtitle = NULL,
  x_lab = NULL
)
}
\arguments{
\item{x}{A fitted model or a tidy data frame of estimates.}

\item{conf_level}{Confidence/credible level, passed to \code{\link[=tidy_estimates]{tidy_estimates()}}
when \code{x} is a model.}

\item{intercept}{Whether to keep the intercept term. Defaults to \code{FALSE},
since the intercept is seldom of interest on a forest plot and its scale
often overwhelms the other terms.}

\item{order}{Order the terms by estimate: \code{"none"} (keep input order),
\code{"ascending"} or \code{"descending"}.}

\item{labels}{Optional display labels for the terms. Either a character
vector the same length as the number of terms (in plotting order) or a
named vector mapping raw term names to labels. If \code{NULL}, names are tidied
with \code{\link[=format_terms]{format_terms()}}.}

\item{interaction}{Passed to \code{\link[=format_terms]{format_terms()}} to control how interaction
terms are rendered (ignored when \code{labels} is supplied).}

\item{point_colour, reference_colour}{Colours for the estimates and the
reference line.}

\item{reference_line}{Position of a vertical reference line (e.g. \code{0} for
differences, \code{1} for odds/risk ratios). Use \code{NA} to omit it.}

\item{point_size, line_size}{Size of the points and interval lines.}

\item{facet}{Whether to give each term its own panel with a free x-axis,
laid out one per row. This removes the squish that occurs when terms live
on very different scales (for example a large intercept alongside small
slopes). Defaults to \code{FALSE}, preserving the shared-axis layout. A
convenience alias for \code{scales = "free"}.}

\item{scales}{Either \code{"fixed"} (the default, a single shared x-axis) or
\code{"free"} (one free-scaled panel per term). When \code{facet = TRUE} this is
forced to \code{"free"}.}

\item{standardize}{Whether to standardise the coefficients by multiplying
each by the standard deviation of its predictor column, putting them on a
common scale so their magnitudes are comparable (and removing the empty
band that otherwise appears when predictors are on very different scales).
Requires a fitted model (ignored, with a warning, for a tidy data frame).
Defaults to \code{FALSE}.}

\item{title, subtitle, x_lab}{Plot title, subtitle and x-axis label. \code{x_lab}
defaults to "Estimate", or "Standardised estimate" when \code{standardize}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws a horizontal point-and-interval ("forest") plot of model estimates.
The input can be a fitted model (anything \code{\link[=tidy_estimates]{tidy_estimates()}} understands) or
a data frame of pre-computed estimates.
}
\examples{
fit <- lm(yield ~ rainfall + fertilizer + soil_ph + treatment,
          data = crop_yield)
coefficient_plot(fit)

# Order terms and add a title
coefficient_plot(fit, order = "descending", title = "Drivers of crop yield")

# When an intercept or large term squishes the rest, give each term its own
# free-scaled panel:
coefficient_plot(fit, intercept = TRUE, facet = TRUE)

# Or put the coefficients on a common, comparable scale:
coefficient_plot(fit, standardize = TRUE)
}
