% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random_effects_plot.R
\name{random_effects_plot}
\alias{random_effects_plot}
\title{Caterpillar plot of random effects}
\usage{
random_effects_plot(
  x,
  conf_level = 0.95,
  sort = TRUE,
  point_colour = depictr_brand(),
  title = NULL,
  x_lab = "Random effect"
)
}
\arguments{
\item{x}{Either a mixed model fitted with 'lme4' (\code{merMod}), or a data frame
with one row per group level and columns such as \code{level}/\code{group},
\code{estimate}, and either \code{conf.low}/\code{conf.high} or \code{std.error}. An optional
\code{term} column facets the plot.}

\item{conf_level}{Confidence level when intervals are derived from standard
errors.}

\item{sort}{Whether to order the levels by their estimate.}

\item{point_colour}{Colour for the points and intervals.}

\item{title, x_lab}{Plot title and value-axis label.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Displays the conditional modes ("BLUPs") of a mixed model's random effects as
a sorted point-and-interval ("caterpillar") plot. It is the usual way to
inspect by-group departures from the average, and to identify unusual groups.
}
\examples{
# From a data frame of by-group estimates:
re <- data.frame(
  level = paste0("G", 1:10),
  estimate = sort(rnorm(10)),
  std.error = runif(10, 0.2, 0.5)
)
random_effects_plot(re)

\donttest{
if (requireNamespace("lme4", quietly = TRUE)) {
  m <- lme4::lmer(RT ~ condition + (1 | participant), data = lexical_decision)
  random_effects_plot(m)
}
}
}
