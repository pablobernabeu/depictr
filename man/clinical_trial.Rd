% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{clinical_trial}
\alias{clinical_trial}
\title{Simulated two-arm clinical trial}
\format{
A data frame with 300 rows and 7 variables:
\describe{
\item{patient}{Patient identifier.}
\item{arm}{Treatment arm (factor): \code{placebo} or \code{treatment}.}
\item{age}{Age in years at enrolment.}
\item{biomarker}{Standardised baseline laboratory value.}
\item{time}{Follow-up time, in months.}
\item{event}{Event indicator: 1 event observed, 0 right-censored.}
\item{adverse_event}{Rare adverse-event indicator: 1 yes, 0 no
(about 10\% positive).}
}
}
\source{
Simulated; see \code{data-raw/generate_datasets.R}.
}
\usage{
clinical_trial
}
\description{
A reproducibly simulated randomised clinical trial, for time-to-event
(survival) and imbalanced-classification examples. The two arms have a real
survival difference: the \code{treatment} arm has a lower hazard, so the
Kaplan-Meier curves separate clearly and the log-rank test is highly
significant, and - because its event times are longer - the treatment arm is
more often right-censored by the 36-month study end. \code{age} and \code{biomarker}
also affect the hazard. The data feed \code{\link[=survival_plot]{survival_plot()}} (pass \code{time},
\code{event} and \code{arm}, or a \code{survival::survfit} object).
}
\details{
\code{adverse_event} is a deliberately rare binary safety outcome (about a 10\%
base rate) that is predictable from \code{arm}, \code{age} and \code{biomarker}. It gives
the precision-recall (\code{\link[=pr_curve_plot]{pr_curve_plot()}}), cumulative-gains (\code{\link[=gain_plot]{gain_plot()}}),
lift (\code{\link[=lift_plot]{lift_plot()}}) and calibration (\code{\link[=calibration_plot]{calibration_plot()}}) demos a
genuinely imbalanced target, where those charts are most informative.

The data are synthetic, generated by \code{data-raw/generate_datasets.R} with a
fixed seed; they do not describe any real individuals.
}
\keyword{datasets}
