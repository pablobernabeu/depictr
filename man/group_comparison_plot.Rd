% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions_extra.R
\name{group_comparison_plot}
\alias{group_comparison_plot}
\title{Compare group means with confidence intervals}
\usage{
group_comparison_plot(
  data,
  y,
  group,
  conf_level = 0.95,
  show_points = TRUE,
  point_alpha = 0.25,
  palette = NULL,
  title = NULL,
  x_lab = NULL,
  y_lab = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y}{The numeric outcome (string or unquoted name).}

\item{group}{The grouping variable (string or unquoted name).}

\item{conf_level}{Confidence level for the intervals (t-based).}

\item{show_points}{Whether to draw the raw data behind the means.}

\item{point_alpha}{Transparency of the raw points.}

\item{palette}{Colours for the groups; defaults to \code{\link[=depictr_palette]{depictr_palette()}}.}

\item{title, x_lab, y_lab}{Title and axis labels.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
An estimation-style plot of a numeric outcome across the levels of a grouping
variable: each group's mean with a confidence interval, over a backdrop of
the raw (jittered) data. By showing both the estimate and its uncertainty, it
conveys whether the groups differ more faithfully than a bar chart does.
}
\details{
A group with a single observation has no degrees of freedom for a t-based
interval, so only its mean is drawn (no interval) and a warning is issued.
}
\examples{
group_comparison_plot(lexical_decision, RT, condition)
group_comparison_plot(crop_yield, yield, treatment)
}
