% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{cluster_plot}
\alias{cluster_plot}
\title{Cluster scatter plot}
\usage{
cluster_plot(
  data,
  cols = NULL,
  k = 3,
  clusters = NULL,
  scale = TRUE,
  hulls = TRUE,
  label_centers = TRUE,
  point_alpha = 0.8,
  palette = NULL,
  title = NULL,
  seed = NULL,
  nstart = 10,
  iter.max = 10
)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Numeric columns to cluster on (default: all numeric).}

\item{k}{Number of clusters for k-means (ignored if \code{clusters} is supplied).}

\item{clusters}{Optional vector of cluster assignments (e.g. from
\code{\link[stats:kmeans]{stats::kmeans()}} or \code{\link[stats:cutree]{stats::cutree()}}); use this to plot a clustering you
computed yourself. Must have exactly one entry per row of \code{data} (rows with
missing values in \code{cols} are dropped from both before plotting).}

\item{scale}{Whether to scale variables to unit variance before clustering and the PCA.}

\item{hulls}{Whether to draw a shaded convex hull around each cluster.}

\item{label_centers}{Whether to label the cluster centroids.}

\item{point_alpha}{Point transparency.}

\item{palette}{Colours for the clusters; defaults to \code{\link[=depictr_palette]{depictr_palette()}}.}

\item{title}{Plot title.}

\item{seed}{Optional integer seed for reproducible k-means (ignored when
\code{clusters} is supplied). Default \code{NULL} leaves the RNG state untouched.}

\item{nstart}{Number of random starts for \code{\link[stats:kmeans]{stats::kmeans()}}.}

\item{iter.max}{Maximum iterations for \code{\link[stats:kmeans]{stats::kmeans()}}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Runs k-means clustering on the numeric columns of a data frame (or uses a
supplied cluster assignment) and plots the observations on the first two
principal components, coloured by cluster, with optional convex hulls and
labelled centroids. Projecting onto principal components means the plot works
for any number of variables.
}
\examples{
cluster_plot(crop_yield, cols = c("rainfall", "fertilizer", "soil_ph",
                                  "yield"), k = 3, seed = 1)
}
