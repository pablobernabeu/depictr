% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_distribution.R
\name{explore_distribution}
\alias{explore_distribution}
\title{Plot the distribution of a variable}
\usage{
explore_distribution(
  data,
  x,
  group = NULL,
  type = c("histogram", "density", "both"),
  bins = 30,
  alpha = 0.6,
  position = NULL,
  palette = NULL,
  facet = FALSE,
  title = NULL,
  x_lab = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{The continuous variable to display. Either a string or an unquoted
column name.}

\item{group}{Optional grouping variable (string or unquoted name) mapped to
colour/fill.}

\item{type}{One of \code{"histogram"}, \code{"density"} or \code{"both"}.}

\item{bins}{Number of histogram bins.}

\item{alpha}{Fill transparency (useful when groups overlap).}

\item{position}{Histogram position adjustment, e.g. \code{"identity"},
\code{"stack"} or \code{"dodge"}. The default (\code{NULL}) chooses \code{"dodge"} when
\code{group} is set (so overlapping bars stay readable) and \code{"identity"}
otherwise.}

\item{palette}{Colours for the groups; defaults to \code{\link[=depictr_palette]{depictr_palette()}}.}

\item{facet}{When a \code{group} is given, draw one panel per group instead of
overlaying them. This is much clearer than an overlay once there are more
than two or three groups (overlaid histograms in particular become hard to
read). Defaults to \code{FALSE}. Ignored when there is no \code{group}.}

\item{title, x_lab}{Plot title and x-axis label (defaults to the variable
name).}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws a histogram, a density curve, or both, optionally split by a grouping
variable. A quick first look at a continuous variable.
}
\examples{
explore_distribution(lexical_decision, RT)
explore_distribution(lexical_decision, RT, group = condition, type = "density")
# One panel per group keeps many groups legible:
explore_distribution(wellbeing_survey, life_satisfaction, group = region,
                     type = "both", facet = TRUE)
}
