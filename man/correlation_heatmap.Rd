% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation_heatmap.R
\name{correlation_heatmap}
\alias{correlation_heatmap}
\title{Plot a correlation matrix as a heatmap}
\usage{
correlation_heatmap(
  data,
  cols = NULL,
  method = "pearson",
  use = "pairwise.complete.obs",
  show_values = TRUE,
  digits = 2,
  palette = depictr_palette(5, "diverging")[c(1, 3, 5)],
  reorder = FALSE,
  title = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Optional character vector of columns to include. If \code{NULL}, all
numeric columns are used.}

\item{method}{Correlation method: \code{"pearson"}, \code{"spearman"} or \code{"kendall"}.}

\item{use}{Missing-value handling passed to \code{\link[stats:cor]{stats::cor()}}.}

\item{show_values}{Whether to annotate each cell with its correlation.}

\item{digits}{Number of decimal places for the annotations.}

\item{palette}{Length-3 vector of colours for the lowest, mid (zero) and
highest correlations. Defaults to the endpoints and midpoint of the
colourblind-aware \code{\link[=depictr_palette]{depictr_palette()}} diverging ramp (negative
correlations red, zero neutral, positive correlations brand blue).}

\item{reorder}{Whether to reorder the variables by hierarchical clustering of
the correlation matrix (using \eqn{1 - r} as the distance), so that blocks
of mutually correlated variables sit together and structure is easier to
see. Defaults to \code{FALSE} (the order of \code{cols}). Skipped with a message if
any correlation is undefined (\code{NA}).}

\item{title}{Plot title.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Computes the pairwise correlations between the numeric columns of a data
frame and displays them as a colour-coded heatmap, optionally annotated with
the correlation values.
}
\details{
Columns with (near-)zero variance cannot be correlated and are
dropped automatically with an informative message, so the raw
\code{"the standard deviation is zero"} warning from \code{\link[stats:cor]{stats::cor()}} is not
surfaced. If, after dropping them, any cells still come out \code{NA} (e.g. two
variables that never co-occur under \code{"pairwise.complete.obs"}), those cells
are rendered in grey and labelled \code{n/a} rather than left blank.
}
\examples{
correlation_heatmap(wellbeing_survey)
correlation_heatmap(crop_yield, method = "spearman", show_values = FALSE)
# Cluster correlated variables together:
correlation_heatmap(wellbeing_survey, reorder = TRUE)
}
